namespace: sd-test
name: example-template
version: '1.0.0'
description: An example pipeline template for testing golang files
maintainer: foo@bar.com
config:
  annotations:
    screwdriver.cd/restrictPR: none
    screwdriver.cd/chainPR: false
  parameters:
    nameA: "value1"
  subscribe:
    scmUrls:
    - https://github.com/VonnyJap/test-pr-config-second.git: ['~commit', '~pr']
  shared:
    image: golang
  jobs:
    main:
      steps:
        - name: echo "pipeline template test"
